apiVersion: v1
kind: Service
metadata:
  name: {{ include "hexxaops.fullname" . }}
  labels:
    {{- include "hexxaops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hexxaops.selectorLabels" . | nindent 4 }}
